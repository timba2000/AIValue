{
  "name": "workspace",
  "version": "1.0.0",
  "private": true,
  "description": "AIValue Use Case Tracker monorepo",
  "scripts": {
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm run build && cd ../backend && npm run build"
  },
  "dependencies": {
    "@types/multer": "^2.0.0",
    "exceljs": "^4.4.0",
    "mammoth": "^1.11.0",
    "multer": "^2.0.2",
    "openai": "^6.14.0",
    "pdf-parse": "^2.4.5",
    "react-markdown": "^10.1.0",
    "xlsx": "^0.18.5"
  }
}
